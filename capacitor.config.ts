import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brilliance.universe', // (I highly recommend changing this from com.example.app before you build!)
  appName: 'brilliance-universe',
  webDir: 'out' // ✅ CHANGED TO 'out'
};

export default config;